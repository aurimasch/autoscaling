apiVersion: custompodautoscaler.com/v1
kind: CustomPodAutoscaler
metadata:
  name: http-request-autoscaler
spec:
  template:
    spec:
      containers:
      - name: http-request-autoscaler
        image: olesiapoz/http-request-autoscaler:latest
        imagePullPolicy: Always
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: demo1
  config:
    - name: interval
      value: "10000"
